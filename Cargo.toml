[package]
name = "liecharts"
version = "0.1.0"
edition = "2024"

[dependencies]
vello_cpu = "0.0.7"
parley = "0.9.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
image = { version = "0.25", default-features = false, features = ["png"] }
thiserror = "2.0"

[dev-dependencies]
anyhow = "1.0"


[workspace]
members = ["site"]
